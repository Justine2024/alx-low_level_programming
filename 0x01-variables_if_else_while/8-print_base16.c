#include <stdio.h>

/**
 * main -Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	char la;
	for (i = '0'; i <= '9i'; i++)
	{
		putchar(i);
	}
	for (la='a'; la<='f';la++)
	{
		putchar(la);
	}
	putchar('\n');
	return (0);
}

