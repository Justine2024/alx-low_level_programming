#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints alphabetics
 * Return: Always 0 (Success)
 */

int main(void)
{
	char d;

	for (d = 'a'; d <= 'z'; d++)
	putchar(d);
	putchar('\n');
	return (0);
}
