#include "main.h"

/**
 * _isdigit - check for a digit
 * @x: the number to be checked
 * Return: 1 for digit or 0 for else
 */

int _isdigit(int x)
{
	if (x >= 49 && x <= 57)
	{
	return (1);
	}
	return (0);
}
