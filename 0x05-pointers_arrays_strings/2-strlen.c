#include "main.h"
/**
 * _strlen - gives the length of a string
 * @s: string
 * Return: Length
 */

int _strlen(char *s)
{
	int lingi = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	return (longi);
}
